// This module will load a story based on a specific _id passed into it through props
import React from "react";

export default function StoryFull(props) {
    return (<div>Full Story Module WIP.</div>);
}